schema: spec-driven
created: 2026-07-13
goal: Move instance config from renderer localStorage to main-process
  electron-store; add safeStorage wrapper; extend IPC contract; one-time
  migration of v1 localStorage keys

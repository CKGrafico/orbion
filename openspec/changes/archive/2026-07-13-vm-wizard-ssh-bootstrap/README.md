# vm-wizard-ssh-bootstrap

Add-VM wizard that runs over SSH: parse SSH config / accept user@host, probe Node version managers, install/reuse daemon + OpenCode, forward tunnel with keepalives, pair, and save the environment.

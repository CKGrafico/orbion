# gh-165-stale-config-warning

Versioned config writes with stale-overwrite warning. The StaleConfigWarning dialog
component shows a modal when a stamp-checked write detects that config was modified
on another machine. This is a visible UI change — the dialog offers Pull remote and
Overwrite anyway buttons.
